version: 2
models:
  - name: stg_movies
    columns: 
      - name: movie_id
        tests:
          - unique
          - not_null
  - name: stg_connection_tag
    columns: 
      - name: tag_id
        tests:
          - unique
          - not_null
      
